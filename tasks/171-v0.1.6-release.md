# Task 171 - v0.1.6 Release

## 状态

Done.

## 发布范围

- 审计事件 `duration_ms` 字段落地（Task 170）。
- Completions 审计与 fallback 对等修复（Task 169）。
- Completions 流式 `[DONE]` 哨兵 JSON 编码修复。
- 测试编译错误修复（`countingCompletionProvider`、`responseFunctionStateProvider` 等）。

## 验收

- `make release-check VERSION=0.1.6` 在 WSL `Ubuntu-24.04` 中通过。
- Changelog 从 Unreleased 移动到 `## 0.1.6 - 2026-07-21`。
- `docs/release.md` 示例版本号更新为 0.1.6。
- 发布提交同步到 `origin/master` 后创建 annotated tag `v0.1.6`。
