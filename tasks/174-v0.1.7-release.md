# Task 174 - v0.1.7 Release

## 状态

Done.

## 发布范围

- `POST /v1/images/generations`（Task 172）。
- `POST /v1/moderations`（Task 173）。

## 验收

- `make release-check VERSION=0.1.7` 通过。
- 发布提交同步到 `origin/master` 后创建 annotated tag `v0.1.7`。
