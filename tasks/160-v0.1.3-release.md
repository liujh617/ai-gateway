# Task 160 - v0.1.3 Release

## Status

Done.

## Release scope

- Stateless text-only `/v1/responses` compatibility.
- Responses function tools and typed function-call streaming.
- In-memory Responses `previous_response_id` continuation with bounded TTL/LRU storage.
- Safe response-store metrics, access-log boolean state, audit previous response ID, and offline smoke coverage.

## Acceptance

- WSL `make release-check VERSION=0.1.3` passes.
- Changelog moves the Responses entries from `Unreleased` to `0.1.3 - 2026-07-13`.
- Release commit is pushed to `origin/master`.
- Annotated tag `v0.1.3` is created and pushed.
