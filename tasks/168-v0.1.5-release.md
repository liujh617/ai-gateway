# Task 168 - v0.1.5 Release

## 状态

Done.

## 发布范围

- Legacy `/v1/completions` 端点，支持 JSON 和 SSE streaming（Task 167）。

## 验收

- `make release-check VERSION=0.1.5` 在 WSL `Ubuntu-24.04` 中通过。
- Changelog 从 Unreleased 移动到 `## 0.1.5 - 2026-07-20`。
- `docs/release.md` 示例版本号更新为 0.1.5。
- 发布提交同步到 `origin/master` 后创建 annotated tag `v0.1.5`。
