# Task 166 - v0.1.4 Release

## 状态

Done.

## 发布范围

- 单模型元数据检索 `GET /v1/models/{model}`（Task 163）。
- Response 读取 `GET/HEAD /v1/responses/{response_id}`（Task 164）。
- Response 删除 `DELETE /v1/responses/{response_id}`（Task 165）。
- response store 锁持有时间优化（Task 161）。
- provider health 读锁优化（Task 162）。

## 验收

- `make release-check VERSION=0.1.4` 在 WSL `Ubuntu-24.04` 中通过。
- Changelog 从 Unreleased 移动到 `## 0.1.4 - 2026-07-16`。
- `docs/release.md` 示例版本号更新为 0.1.4。
- 发布提交同步到 `origin/master` 后创建 annotated tag `v0.1.4`。
